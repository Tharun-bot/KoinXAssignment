# copy this to .env and change as needed
# (or just set env vars directly)

MONGO_URL=mongodb://localhost:27017/crypto_reconciler
PORT=3000

# matching tolerances
TIMESTAMP_TOLERANCE_SECONDS=300
QUANTITY_TOLERANCE_PCT=0.01
